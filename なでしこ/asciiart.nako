# asciiart なでしこ3
yを-12から12まで繰り返す
    line = ""
    xを-39から39まで繰り返す
        ca = x * 0.0458
        cb = y * 0.08333
        a = ca
        b = cb
        s = " "
        iを0から15まで繰り返す
            t = a * a - b * b + ca
            b = 2 * a * b + cb
            a = t
            もし、(a * a + b * b) > 4ならば
                "0123456789ABCDEF"でi+1から1文字抜出す
                sはそれ
                抜ける
            ここまで
        ここまで
        line = line & s
    ここまで
    lineを表示
ここまで
